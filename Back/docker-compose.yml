version: '3'
services:
  cot-api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            capabilities: [gpu]
    command: ["tail", "-f", "/dev/null"]